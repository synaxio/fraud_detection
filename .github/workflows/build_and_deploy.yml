name: Build and Deploy to HuggingFace

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout repo
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Install Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install MLflow + boto3
      run: pip install mlflow boto3

    - name: Download latest MLflow model
      env:
        MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
        MLFLOW_TRACKING_TOKEN: ${{ secrets.MLFLOW_TRACKING_TOKEN }}
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
        MLFLOW_S3_ENDPOINT_URL: ${{ secrets.MLFLOW_S3_ENDPOINT_URL }}
      run: |
        python - <<EOF
        import mlflow
        import os
        import shutil
        from mlflow.tracking import MlflowClient

        EXPERIMENT_NAME = 'fraud_detector'

        # Récupérer l'expérience
        exp = mlflow.get_experiment_by_name(EXPERIMENT_NAME)
        exp_id = exp.experiment_id

        # Chercher le dernier run réussi
        runs = mlflow.search_runs(
            [exp_id], 
            filter_string="attributes.status = 'FINISHED'",        
            order_by=["attributes.start_time DESC"],
            max_results=1
        )
        
        last_run_id = runs.iloc[0]["run_id"]
        print(f"Last run ID: {last_run_id}")

        # Lister les artifacts pour vérification
        client = MlflowClient()
        artifacts = client.list_artifacts(last_run_id)
        print("Available artifacts:")
        for a in artifacts:
            print(f"  - {a.path}")

        # Supprimer le dossier précédent
        shutil.rmtree("model", ignore_errors=True)
        print("Old model directory removed.")

        # Télécharger depuis le dossier 'code'
        model_uri = f"runs:/{last_run_id}/code"
        print(f"Downloading from: {model_uri}")
        
        local_path = mlflow.artifacts.download_artifacts(
            artifact_uri=model_uri,
            dst_path="."
        )
        
        print(f"Downloaded to: {local_path}")
        
        # Renommer 'code' en 'model' pour correspondre à votre app
        if os.path.exists("code"):
            if os.path.exists("model"):
                shutil.rmtree("model")
            shutil.move("code", "model")
            print("Renamed 'code' to 'model'")
        
        # Vérifier que MLmodel existe
        if not os.path.exists("model/MLmodel"):
            raise Exception("MLmodel file not found in downloaded artifacts!")
        
        print("✓ Model downloaded successfully")
        
        # Afficher la structure
        print("\n=== Model structure ===")
        for root, dirs, files in os.walk("model"):
            level = root.replace("model", "").count(os.sep)
            indent = " " * 2 * level
            print(f"{indent}{os.path.basename(root)}/")
            subindent = " " * 2 * (level + 1)
            for file in files[:5]:  # Limiter à 5 fichiers par dossier
                print(f"{subindent}{file}")
            if len(files) > 5:
                print(f"{subindent}... and {len(files) - 5} more files")
        
        EOF
    
    - name: Initialize HuggingFace Space if needed
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
        pip install huggingface_hub
        
        python - <<EOF
        from huggingface_hub import HfApi
        
        api = HfApi(token="${{ secrets.HF_TOKEN }}")
        repo_id = "${{ secrets.HF_USERNAME }}/api-prediction"
        
        try:
            # Vérifier si le README existe
            api.hf_hub_download(repo_id=repo_id, filename="README.md", repo_type="space")
            print("Space déjà initialisé")
        except:
            print("Initialisation du Space...")
            # Créer le README.md initial
            readme_content = """---
        title: API Prediction
        emoji: 🔮
        colorFrom: blue
        colorTo: green
        sdk: docker
        app_port: 7860
        pinned: false
        ---
        
        # API Prediction
        
        API de prédiction déployée automatiquement via GitHub Actions.
        """
            api.upload_file(
                path_or_fileobj=readme_content.encode(),
                path_in_repo="README.md",
                repo_id=repo_id,
                repo_type="space",
                token="${{ secrets.HF_TOKEN }}"
            )
            print("Space initialisé avec succès")
        EOF
    
    - name: Login HuggingFace registry
      run: |
        echo "${{ secrets.HF_TOKEN }}" | docker login -u ${{ secrets.HF_USERNAME }} --password-stdin registry.hf.space

    - name: Build image
      run: |
        # Vérifiez que le nom correspond EXACTEMENT à votre Space existant
        docker build -t registry.hf.space/${{ secrets.HF_USERNAME }}/api-prediction -f docker/Dockerfile .

    - name: Deploy to HuggingFace Space
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
            git config --global user.email "github-actions@github.com"
            git config --global user.name "GitHub Actions"
            
            git clone https://oauth2:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/${{ secrets.HF_USERNAME }}/api-prediction space_repo
            cd space_repo
            
            # Copier le Dockerfile
            cp ../docker/Dockerfile ./Dockerfile
            
            # Copier le dossier model
            rm -rf model
            cp -r ../model ./model
            
            # Copier le dossier api (IMPORTANT!)
            rm -rf api
            cp -r ../api ./api
            
            # Copier requirements.txt
            cp ../requirements.txt ./requirements.txt
            
            # Copier tout autre fichier nécessaire mentionné dans votre Dockerfile
            # Par exemple, si vous avez un main.py, app.py, etc.
            # cp ../main.py ./main.py
            
            cat > README.md <<'EOL'
            ---
            title: API Prediction
            emoji: 🔮
            colorFrom: blue
            colorTo: green
            sdk: docker
            app_port: 7860
            pinned: false
            ---
            EOL
            
            git add .
            git diff --staged --quiet || (git commit -m "Deploy from GitHub Actions - $(date '+%Y-%m-%d %H:%M:%S')" && git push)
